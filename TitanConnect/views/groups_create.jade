extends layout

block content
    h2 Create Group
    form(role='form', action="/groups/create",method="post", style='max-width: 300px;')
        .form-group
            input(type='text', name="groupname", placeholder='Enter Group Name')
        .form-group
            input(type='text', name="description", placeholder='What is your group all about?')
        button(type='submit') Submit
